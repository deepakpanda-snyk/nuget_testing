<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <!-- Also resolves via the main feed ("*" mapping). -->
    <PackageReference Include="Contoso.Utils" />
  </ItemGroup>

</Project>
